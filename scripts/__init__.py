"""Scripts that produce or collect a result reported in the paper.

Each module here backs a table, figure or baseline column; see REPRODUCIBILITY.md
for which result each one produces. One-off repair helpers for the authors' own
run artifacts are deliberately not kept.
"""
